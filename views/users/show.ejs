<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <header>
      <% include ../partials/header %>
    </header>
    <div class="users">
<!-- main start-->

<!-- views/users/show.ejs -->
<div class="show">
  <div class="buttons floats">
    <button onclick="location.href='/'">Home</button>
    <button onclick="location.href='/users/<%=user._id%>/edit'" class="float_right">수정</button>
  </div>
  <h3>사용자 정보</h3>
  <div class="inputs">
    <div class="floats">
      <label>Email</label>
      <input disabled type="text" value="<%= user.email %>">
    </div>
    <div class="floats">
      <label>별명</label>
      <input disabled type="text" value="<%= user.nickname %>">
    </div>
  </div>
</div>

<!-- main end -->
    </div><!--users end -->
    <footer>
      <% include ../partials/footer %>
    </footer>
  </body>
</html>
